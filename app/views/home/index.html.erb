    <div id="home-page" class="min-h-screen bg-gradient-to-br from-indigo-50 via-white to-purple-50">
  <!-- Hero Section -->
  <div class="relative overflow-hidden">
      <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-16 sm:py-20 md:py-24">
      <div class="text-center">
        <!-- Hero Icon -->
        <div class="mx-auto h-20 w-20 mb-8 flex items-center justify-center rounded-full bg-gradient-to-r from-indigo-500 to-purple-600">
          <svg class="h-10 w-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.246 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"></path>
          </svg>
        </div>

        <!-- Hero Headline -->
        <h1 class="text-3xl sm:text-4xl md:text-5xl font-bold text-gray-900 mb-4 sm:mb-6 leading-tight">
          What should you read next?
        </h1>
        
        <!-- Hero Subtitle -->
        <p class="text-base sm:text-lg md:text-xl text-gray-600 mb-8 sm:mb-12 max-w-2xl mx-auto px-2">
          Tell us your mood, we'll find your next book in 30 seconds. 
          <span class="font-semibold text-indigo-600">No account required.</span>
        </p>

        <!-- Mood Input Form - Sleek Design -->
        <div class="max-w-xl mx-auto mb-8 sm:mb-12">
          <!-- Enhanced Form for All Devices -->
          <div id="main-form" class="main-form">
            <%= form_with url: recommendations_path, method: :post, local: true, class: "space-y-6", data: { turbo: false }, id: "recommendation-form" do %>
              <!-- Context Textarea -->
              <div>
                <label class="block text-sm font-medium text-gray-700 mb-3 text-left">Describe your reading mood</label>
                <%= text_area_tag :context, nil, 
                    rows: 4, 
                    class: "block w-full border-2 border-gray-300 rounded-xl shadow-sm focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 text-base resize-none transition-all duration-200 bg-white",
                    placeholder: "Chargement...",
                    required: true,
                    data: { controller: "placeholder" } %>
                
                
             
                                                                                <!-- Smart Language Tips - Sleek Design -->
                <div class="mt-4 relative" data-controller="tips">
                  <!-- Subtle background with glass effect -->
                  <div class="absolute inset-0 bg-white/40 backdrop-blur-sm rounded-2xl border border-white/60"></div>
                  
                  <!-- Content -->
                  <div class="relative p-5">
                    <!-- Header with minimal icon -->
                    <div class="flex items-center mb-4">
                      <div class="w-6 h-6 bg-gradient-to-r from-indigo-500 to-purple-600 rounded-lg flex items-center justify-center mr-3">
                        <svg class="w-3 h-3 text-white" fill="currentColor" viewBox="0 0 20 20">
                          <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"></path>
                        </svg>
                      </div>
                      <h4 class="text-sm font-semibold text-gray-800 tracking-wide uppercase">Language Tips</h4>
                    </div>
                    
                    <!-- Tips list - clean and minimal -->
                    <div class="space-y-3">
                      <div class="flex items-start">
                        <div class="w-1.5 h-1.5 bg-indigo-400 rounded-full mt-2 mr-3 flex-shrink-0"></div>
                        <span class="text-sm text-gray-700 leading-relaxed">Results are in English by default</span>
                      </div>
                      <div class="flex items-start">
                        <div class="w-1.5 h-1.5 bg-indigo-400 rounded-full mt-2 mr-3 flex-shrink-0"></div>
                        <span class="text-sm text-gray-700 leading-relaxed">Want books in French? Write "livre en français" or "french book"</span>
                      </div>
                      <div class="flex items-start" id="random-example">
                        <div class="w-1.5 h-1.5 bg-indigo-400 rounded-full mt-2 mr-3 flex-shrink-0"></div>
                        <span class="text-sm text-gray-700 leading-relaxed">Examples: "Je veux un roman français" or "Quiero un libro en español"</span>
                      </div>
                      <div class="flex items-start">
                        <div class="w-1.5 h-1.5 bg-indigo-400 rounded-full mt-2 mr-3 flex-shrink-0"></div>
                        <span class="text-sm text-gray-700 leading-relaxed">Write your mood in any language you're comfortable with!</span>
                      </div>
                    </div>
                  </div>
                </div>
              </div>

              <!-- Compact Tone Chips -->
              <div>
                <label class="block text-sm font-medium text-gray-700 mb-2 text-left">Tone & Style</label>
                <div class="flex flex-wrap gap-2 justify-center">
                  <% ['Deep dive', 'Fast-paced', 'Optimistic', 'Thoughtful', 'Adventure', 'Cozy', 'Challenging', 'Light'].each do |tone| %>
                    <label class="relative">
                      <%= check_box_tag 'tone_chips[]', tone, false, class: 'sr-only' %>
                      <span class="inline-flex items-center justify-center px-3 py-2 rounded-lg text-sm font-medium bg-white border-2 border-gray-200 text-gray-700 cursor-pointer hover:bg-indigo-50 hover:border-indigo-300 hover:text-indigo-700 transition-all duration-200 active:scale-95 shadow-sm">
                        <%= tone %>
                      </span>
                    </label>
                  <% end %>
                </div>
              </div>

              <!-- Include Reading History Toggle - Only for authenticated users -->
              <% if user_signed_in? %>
                <% 
                  # Récupérer les statistiques de la librairie
                  library_stats = current_user.user_readings.group(:status).count
                  total_books = library_stats.values.sum
                  read_books = library_stats['read'] || 0
                  to_read_books = library_stats['to_read'] || 0
                  reading_books = library_stats['reading'] || 0
                %>
                
                <!-- My Library Section - Sleek Design -->
                <div class="mt-8 bg-white/80 backdrop-blur-sm rounded-3xl border border-white/20 shadow-xl p-8">
                  <div class="text-center mb-8">
                    <h3 class="text-2xl font-bold text-gray-900 mb-2">Your Library</h3>
                    <p class="text-gray-600">Personalized recommendations based on your reading history</p>
                  </div>

                  <!-- Sleek Stats Display -->
                  <div class="flex justify-center mb-8">
                    <div class="inline-flex items-center space-x-8 bg-gradient-to-r from-indigo-50 to-purple-50 rounded-2xl px-6 py-4 border border-indigo-100">
                      <div class="text-center">
                        <div class="text-3xl font-bold text-indigo-600"><%= total_books %></div>
                        <div class="text-sm text-indigo-700 font-medium">Total Books</div>
                      </div>
                      <div class="w-px h-12 bg-indigo-200"></div>
                      <div class="text-center">
                        <div class="text-2xl font-bold text-green-600"><%= read_books %></div>
                        <div class="text-sm text-green-700 font-medium">Completed</div>
                      </div>
                      <div class="w-px h-12 bg-indigo-200"></div>
                      <div class="text-center">
                        <div class="text-2xl font-bold text-blue-600"><%= to_read_books %></div>
                        <div class="text-sm text-blue-700 font-medium">To Read</div>
                      </div>
                    </div>
                  </div>

                  <!-- Elegant Action Toggle -->
                  <div class="max-w-md mx-auto">
                    <label class="relative flex items-center justify-center cursor-pointer group">
                      <input type="checkbox" 
                             class="sr-only peer" 
                             onchange="toggleHistoryActions(this)">
                      <div class="relative w-16 h-8 bg-gray-300 peer-focus:outline-none peer-focus:ring-4 peer-focus:ring-indigo-300 rounded-full peer peer-checked:after:translate-x-full peer-checked:after:border-white after:content-[''] after:absolute after:top-[2px] after:left-[2px] after:bg-white after:border-2 after:border-gray-400 after:rounded-full after:h-7 after:w-7 after:transition-all peer-checked:bg-indigo-600 shadow-sm"></div>
                      <span class="ml-4 text-sm font-medium text-gray-700 group-hover:text-gray-900 transition-colors">
                        Use my reading history
                      </span>
                    </label>
                  </div>

                  <!-- Sleek Action Buttons - Hidden by default -->
                  <div id="history-actions" class="mt-6 hidden">
                    <div class="flex flex-col sm:flex-row gap-3 justify-center">
                      <% if total_books > 0 %>
                        <!-- User has books - show import option -->
                        <%= link_to new_import_path,
                            class: "inline-flex items-center justify-center px-6 py-3 text-sm font-medium text-indigo-700 bg-indigo-50 border border-indigo-200 rounded-xl hover:bg-indigo-100 hover:border-indigo-300 transition-all duration-200 transform hover:scale-105" do %>
                          <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M9 19l3 3m0 0l3-3m-3 3V10"></path>
                          </svg>
                          Add More Books
                        <% end %>
                      <% else %>
                        <!-- User has no books - strong CTA to import -->
                        <%= link_to new_import_path,
                            class: "inline-flex items-center justify-center px-8 py-4 text-base font-medium text-white bg-gradient-to-r from-indigo-600 to-purple-600 rounded-xl hover:from-indigo-700 hover:to-purple-700 transition-all duration-200 transform hover:scale-105 shadow-lg hover:shadow-xl" do %>
                          <svg class="w-5 h-5 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M9 19l3 3m0 0l3-3m-3 3V10"></path>
                          </svg>
                          🚀 Import Goodreads Now
                        <% end %>
                      <% end %>
                    </div>
                  </div>
                </div>
              <% end %>





              <!-- Submit Button - Enhanced Design with iOS Compatibility -->
              <button type="submit" id="home-submit-button" class="w-full bg-gradient-to-r from-indigo-600 to-purple-600 text-white py-4 px-6 rounded-xl font-semibold text-lg hover:from-indigo-700 hover:to-purple-700 transition-all duration-200 flex items-center justify-center shadow-lg hover:shadow-xl transform hover:scale-105 active:scale-95 relative z-10 border-2 border-indigo-700">
                <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
                </svg>
                Get My Next Book
              </button>
            <% end %>
          </div>


        </div>

        <!-- Quick Tips - Sleek Design -->
        <div class="max-w-xl mx-auto p-4 sm:p-6 bg-blue-50 rounded-xl border border-blue-100">
          <h4 class="text-sm font-medium text-blue-800 mb-3 text-center sm:text-left">💡 Quick Tips</h4>
          <ul class="text-sm text-blue-700 space-y-2 text-center sm:text-left">
            <li class="flex items-center justify-center sm:justify-start">
              <span class="w-2 h-2 bg-blue-400 rounded-full mr-2 flex-shrink-0"></span>
              Mention specific authors or books you love
            </li>
            <li class="flex items-center justify-center sm:justify-start">
              <span class="w-2 h-2 bg-blue-400 rounded-full mr-2 flex-shrink-0"></span>
              Include your preferred reading time (quick reads vs. deep dives)
            </li>
            <li class="flex items-center justify-center sm:justify-start">
              <span class="w-2 h-2 bg-blue-400 rounded-full mr-2 flex-shrink-0"></span>
              Specify if you want to explore new genres or stick to favorites
            </li>
          </ul>
        </div>

        <!-- Trust Indicators -->
        <div class="mt-8 flex flex-col sm:flex-row flex-wrap justify-center items-center gap-4 sm:gap-6 text-sm text-gray-500">
          <div class="flex items-center">
            <svg class="w-4 h-4 mr-2 text-green-500 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
            </svg>
            <span class="whitespace-nowrap">AI-powered recommendations</span>
          </div>
          <div class="flex items-center">
            <svg class="w-4 h-4 mr-2 text-green-500 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
            </svg>
            <span class="whitespace-nowrap">Basic recommendations free</span>
          </div>
          <div class="flex items-center">
            <svg class="w-4 h-4 mr-2 text-green-500 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
            </svg>
            <span class="whitespace-nowrap">Sign up for personalization</span>
          </div>
        </div>
        
        <!-- TEMPORAIREMENT DÉSACTIVÉ: Chat Interface Link -->
        <!-- <div class="mt-8 text-center">
          <p class="text-sm text-gray-500 mb-3">Want to try our mobile chat interface?</p>
          <a href="/recommendations/chat" 
              data-turbo="false"
              class="inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-lg text-gray-700 bg-white hover:bg-gray-50 transition-colors duration-200">
            <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8s9 3.582 9 8z"/>
            </svg>
            Try Chat Interface
          </a>
        </div> -->
        
        <!-- TEMPORAIRE: Message informatif -->
        <div class="mt-8 text-center">
          <p class="text-sm text-gray-500 mb-3">Mobile chat interface temporarily disabled</p>
          <div class="inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-lg text-gray-400 bg-gray-50 cursor-not-allowed">
            <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
            </svg>
            Coming Soon
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- How It Works Section -->
  <div class="py-24 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-16">
        <h2 class="text-3xl font-bold text-gray-900 mb-4">How it works</h2>
        <p class="text-xl text-gray-600">Get personalized book recommendations in 3 simple steps</p>
      </div>

      <div class="grid md:grid-cols-3 gap-8">
        <!-- Step 1 -->
        <div class="text-center">
          <div class="mx-auto h-16 w-16 mb-6 flex items-center justify-center rounded-full bg-indigo-100 text-indigo-600 font-bold text-2xl">1</div>
          <h3 class="text-xl font-semibold text-gray-900 mb-3">Describe your mood</h3>
          <p class="text-gray-600">Tell us what you're in the mood for, your preferred tone, or what you want to explore next.</p>
        </div>

        <!-- Step 2 -->
        <div class="text-center">
          <div class="mx-auto h-16 w-16 mb-6 flex items-center justify-center rounded-full bg-indigo-100 text-indigo-600 font-bold text-2xl">2</div>
          <h3 class="text-xl font-semibold text-gray-900 mb-3">Get instant results</h3>
          <p class="text-gray-600">Our AI analyzes your preferences and gives you a personalized reading brief with 3 perfect book picks.</p>
        </div>

        <!-- Step 3 -->
        <div class="text-center">
          <div class="mx-auto h-16 w-16 mb-6 flex items-center justify-center rounded-full bg-indigo-100 text-indigo-600 font-bold text-2xl">3</div>
          <h3 class="text-xl font-semibold text-gray-900 mb-3">Enhance & personalize</h3>
          <p class="text-gray-600">Sign up to import your reading history, save books, and get even better recommendations.</p>
        </div>
      </div>
    </div>
  </div>

  <!-- Enhanced Recommendations Section -->
  <div class="py-24 bg-gradient-to-r from-indigo-50 to-purple-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-16">
        <h2 class="text-3xl font-bold text-gray-900 mb-4">Enhanced recommendations</h2>
        <p class="text-xl text-gray-600">Share your reading data for personalized suggestions</p>
      </div>

      <div class="grid md:grid-cols-2 gap-8">
        <!-- Import Goodreads -->
        <div class="bg-white rounded-2xl p-8 shadow-lg">
          <div class="flex items-center mb-4">
            <div class="h-12 w-12 bg-green-100 rounded-lg flex items-center justify-center mr-4">
              <svg class="h-6 w-6 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M9 19l3 3m0 0l3-3m-3 3V10"></path>
              </svg>
            </div>
            <div>
              <h3 class="text-xl font-semibold text-gray-900">Import Goodreads</h3>
              <p class="text-gray-600">Upload your reading history for personalized recommendations</p>
            </div>
          </div>
        </div>

        <!-- Manual Entry -->
        <div class="bg-white rounded-2xl p-8 shadow-lg">
          <div class="flex items-center mb-4">
            <div class="h-12 w-12 bg-blue-100 rounded-lg flex items-center justify-center mr-4">
              <svg class="h-6 w-6 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
              </svg>
            </div>
            <div>
              <h3 class="text-xl font-semibold text-gray-900">Add Books Manually</h3>
              <p class="text-gray-600">Build your library by adding books you've read or want to read</p>
            </div>
          </div>
        </div>
      </div>



      <!-- Future Features -->
      <div class="mt-12 text-center">
        <p class="text-gray-600 mb-4">Coming soon: Photo uploads of your reading list for instant library building</p>
        <div class="inline-flex items-center px-4 py-2 bg-gray-100 rounded-full text-sm text-gray-600">
          <svg class="w-4 h-4 mr-2" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm1-12a1 1 0 10-2 0v4a1 1 0 00.293.707l2.828 2.829a1 1 0 101.415-1.415L11 9.586V6z" clip-rule="evenodd"></path>
          </svg>
          Photo uploads coming soon
        </div>
      </div>
    </div>
  </div>

  <!-- Quick Actions for Logged Users -->
  <% if user_signed_in? %>
    <div class="py-16 bg-white">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <h2 class="text-2xl font-bold text-gray-900 mb-8 text-center">Quick Actions</h2>
        <div class="flex flex-wrap justify-center gap-4">
          <%= link_to "View My Library", books_path, 
              class: "inline-flex items-center px-6 py-3 text-base font-medium text-gray-700 bg-gray-100 rounded-lg hover:bg-gray-200 transition-colors duration-200" %>
          <%= link_to "Account Settings", edit_user_registration_path, 
              class: "inline-flex items-center px-6 py-3 text-base font-medium text-gray-700 bg-gray-100 rounded-lg hover:bg-gray-200 transition-colors duration-200" %>
        </div>
      </div>
    </div>
  <% else %>
    <!-- Refine & Discover More Section - For First-Time Users -->
    <div class="py-16 bg-gradient-to-r from-indigo-50 to-purple-50">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-12">
          <h2 class="text-3xl font-bold text-gray-900 mb-4">Refine & Discover More</h2>
          <p class="text-xl text-gray-600">Take your reading journey to the next level</p>
        </div>
        
        <div class="grid md:grid-cols-3 gap-8">
          <!-- Refine Recommendations -->
          <div class="text-center">
            <div class="mx-auto h-16 w-16 mb-4 flex items-center justify-center rounded-full bg-indigo-100">
              <svg class="h-8 w-8 text-indigo-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"></path>
              </svg>
            </div>
            <h3 class="text-lg font-semibold text-gray-900 mb-3">Refine Your Results</h3>
            <p class="text-gray-600 mb-4">Not quite what you're looking for? Adjust your preferences and get new recommendations.</p>
            <button onclick="scrollToTop()" class="inline-flex items-center px-4 py-2 bg-indigo-600 text-white text-sm font-medium rounded-lg hover:bg-indigo-700 transition-colors duration-200">
              Try Different Mood
            </button>
          </div>

          <!-- Explore New Genres -->
          <div class="text-center">
            <div class="mx-auto h-16 w-16 mb-4 flex items-center justify-center rounded-full bg-purple-100">
              <svg class="h-8 w-8 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"></path>
              </svg>
            </div>
            <h3 class="text-lg font-semibold text-gray-900 mb-3">Explore New Genres</h3>
            <p class="text-gray-600 mb-4">Step outside your comfort zone and discover books from genres you haven't tried before.</p>
            <button onclick="suggestNewGenre()" class="inline-flex items-center px-4 py-2 bg-purple-600 text-white text-sm font-medium rounded-lg hover:bg-purple-700 transition-colors duration-200">
              Suggest Genre
            </button>
          </div>

          <!-- Build Your Library -->
          <div class="text-center">
            <div class="mx-auto h-16 w-16 mb-4 flex items-center justify-center rounded-full bg-green-100">
              <svg class="h-8 w-8 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
              </svg>
            </div>
            <h3 class="text-lg font-semibold text-gray-900 mb-3">Build Your Library</h3>
            <p class="text-gray-600 mb-4">Import your Goodreads library or add books manually for even better personalized recommendations.</p>
          </div>
        </div>
      </div>
    </div>
  <% end %>
</div>

<script>
// TEMPORAIREMENT DÉSACTIVÉ: Logique de basculement entre formulaires
// TODO: Réactiver après refactor du chat UX
document.addEventListener('DOMContentLoaded', function() {
  // Function to show correct form based on screen size
  function showCorrectForm() {
    const desktopForm = document.getElementById('desktop-form');
    const mobileCTA = document.getElementById('mobile-cta');
    
    // TEMPORAIRE: Toujours montrer le formulaire principal
    if (desktopForm) desktopForm.style.display = 'block';
    if (mobileCTA) mobileCTA.style.display = 'none';
    
    /* TODO: Réactiver cette logique après refactor
    if (window.innerWidth >= 768) { // md breakpoint
      // Desktop view
      if (desktopForm) desktopForm.style.display = 'block';
      if (mobileCTA) mobileCTA.style.display = 'none';
    } else {
      // Mobile view
      if (desktopForm) desktopForm.style.display = 'none';
      if (mobileCTA) mobileCTA.style.display = 'block';
    }
    */
  }
  
  // Show correct form on load
  showCorrectForm();
  
  // Update on resize (temporairement désactivé)
  // window.addEventListener('resize', showCorrectForm);
  
  // Add form validation and loading states
  // Form submission handling - Target specific recommendation form
  const recommendationForm = document.getElementById('recommendation-form');
  if (recommendationForm) {
    console.log('Recommendation form found, setting up event listener...');
    
    recommendationForm.addEventListener('submit', function(e) {
      console.log('Form submit event triggered');
      
      // Validate context is not empty
      const context = this.querySelector('textarea[name="context"]').value.trim();
      if (!context) {
        e.preventDefault();
        // Use visual feedback instead of alert
        const contextField = this.querySelector('textarea[name="context"]');
        contextField.focus();
        contextField.classList.add('border-red-500', 'ring-2', 'ring-red-200');
        setTimeout(() => {
          contextField.classList.remove('border-red-500', 'ring-2', 'ring-red-200');
        }, 2000);
        return;
      }
      
      console.log('Context validated, showing loading state...');
      
      // Show loading state
      const submitBtn = this.querySelector('button[type="submit"]');
      const originalText = submitBtn.innerHTML;
      submitBtn.innerHTML = '<svg class="animate-spin -ml-1 mr-3 h-5 w-5 text-white" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24"><circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle><path class="opacity-75" fill="currentColor" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path></svg>Getting recommendations...';
      submitBtn.disabled = true;
      
      console.log('Button loading state applied');
      
      // Re-enable button after a delay (in case of errors)
      setTimeout(() => {
        submitBtn.innerHTML = originalText;
        submitBtn.disabled = false;
        console.log('Button reset to original state');
      }, 10000);
    });
    
    console.log('Event listener set up successfully');
  } else {
    console.error('Recommendation form not found!');
  }

  // Handle tone chip selection on home page - Turbo compatible
  function initializeHomeToneChips() {
    const toneChips = document.querySelectorAll('input[name="tone_chips[]"]');
    
    toneChips.forEach(chip => {
      // Remove existing event listeners to prevent duplicates
      const newChip = chip.cloneNode(true);
      chip.parentNode.replaceChild(newChip, chip);
      
      newChip.addEventListener('change', function() {
        const span = this.nextElementSibling;
        if (this.checked) {
          span.classList.remove('bg-gray-50', 'border-gray-200', 'text-gray-600');
          span.classList.add('bg-indigo-100', 'border-indigo-300', 'text-indigo-700');
        } else {
          span.classList.remove('bg-indigo-100', 'border-indigo-300', 'text-indigo-700');
          span.classList.add('bg-gray-50', 'border-gray-200', 'text-gray-600');
        }
      });
    });
  }

  // Function to scroll to top of the page
  function scrollToTop() {
    window.scrollTo({ top: 0, behavior: 'smooth' });
  }

  // Function to suggest a new genre
  function suggestNewGenre() {
    const genres = [
      'Science Fiction', 'Fantasy', 'Mystery', 'Thriller', 'Romance', 
      'Historical Fiction', 'Biography', 'Memoir', 'Self-Help', 'Philosophy',
      'Economics', 'Psychology', 'Travel', 'Cooking', 'Art', 'Music',
      'Poetry', 'Drama', 'Horror', 'Western', 'Young Adult', 'Children'
    ];
    
    const randomGenre = genres[Math.floor(Math.random() * genres.length)];
    const context = document.querySelector('textarea[name="context"]');
    
    if (context) {
      context.value = `I want to explore ${randomGenre} books. Something engaging and well-written that will introduce me to this genre.`;
      context.focus();
      
      // Show toast notification
      showToast(`Try exploring ${randomGenre}! I've updated your request.`, 'success');
    }
  }

  // Toast notification function
  function showToast(message, type = 'info') {
    // Create toast element
    const toast = document.createElement('div');
    toast.className = `fixed top-4 right-4 z-50 px-6 py-3 rounded-lg text-white font-medium transform transition-all duration-300 translate-x-full ${
      type === 'success' ? 'bg-green-500' : 
      type === 'error' ? 'bg-red-500' : 
      'bg-blue-500'
    }`;
    toast.textContent = message;
    
    document.body.appendChild(toast);
    
    // Animate in
    setTimeout(() => {
      toast.classList.remove('translate-x-full');
    }, 100);
    
    // Animate out and remove
    setTimeout(() => {
      toast.classList.add('translate-x-full');
      setTimeout(() => {
        document.body.removeChild(toast);
      }, 300);
    }, 3000);
  }

  // Simple tone chip toggle functionality - Turbo compatible
  function initializeHomeToneChipToggles() {
    const toneChips = document.querySelectorAll('input[name="tone_chips[]"]');
    
    toneChips.forEach(checkbox => {
      // Remove existing event listeners to prevent duplicates
      const newCheckbox = checkbox.cloneNode(true);
      const newSpan = checkbox.nextElementSibling.cloneNode(true);
      checkbox.parentNode.replaceChild(newCheckbox, checkbox);
      newCheckbox.parentNode.replaceChild(newSpan, newCheckbox.nextElementSibling);
      
      const chipSpan = newCheckbox.nextElementSibling;
      
      chipSpan.addEventListener('click', function() {
        newCheckbox.checked = !newCheckbox.checked;
        
        // Visual feedback
        if (newCheckbox.checked) {
          this.classList.add('bg-indigo-100', 'border-indigo-400', 'text-indigo-700');
        } else {
          this.classList.remove('bg-indigo-100', 'border-indigo-400', 'text-indigo-700');
        }
      });
    });
  }

  // Function to toggle history actions visibility
  function toggleHistoryActions(checkbox) {
    const historyActions = document.getElementById('history-actions');
    if (checkbox.checked) {
      historyActions.classList.remove('hidden');
    } else {
      historyActions.classList.add('hidden');
    }
  }
  
      // Initialize tone chips
    initializeHomeToneChips();
    
    
  initializeHomeToneChipToggles();
});

// Re-initialize on Turbo navigation
document.addEventListener('turbo:load', function() {
  // Handle tone chip selection on home page - Turbo compatible
  function initializeHomeToneChips() {
    const toneChips = document.querySelectorAll('input[name="tone_chips[]"]');
    
    toneChips.forEach(chip => {
      // Remove existing event listeners to prevent duplicates
      const newChip = chip.cloneNode(true);
      chip.parentNode.replaceChild(newChip, chip);
      
      newChip.addEventListener('change', function() {
        const span = this.nextElementSibling;
        if (this.checked) {
          span.classList.remove('bg-gray-50', 'border-gray-200', 'text-gray-600');
          span.classList.add('bg-indigo-100', 'border-indigo-300', 'text-indigo-700');
        } else {
          span.classList.remove('bg-indigo-100', 'border-indigo-300', 'text-indigo-700');
          span.classList.add('bg-gray-50', 'border-gray-200', 'text-gray-600');
        }
      });
    });
  }

  // Simple tone chip toggle functionality - Turbo compatible
  function initializeHomeToneChipToggles() {
    const toneChips = document.querySelectorAll('input[name="tone_chips[]"]');
    
    toneChips.forEach(checkbox => {
      // Remove existing event listeners to prevent duplicates
      const newCheckbox = checkbox.cloneNode(true);
      const newSpan = checkbox.nextElementSibling.cloneNode(true);
      checkbox.parentNode.replaceChild(newCheckbox, checkbox);
      newCheckbox.parentNode.replaceChild(newSpan, newCheckbox.nextElementSibling);
      
      const chipSpan = newCheckbox.nextElementSibling;
      
      chipSpan.addEventListener('click', function() {
        newCheckbox.checked = !newCheckbox.checked;
        
        // Visual feedback
        if (newCheckbox.checked) {
          this.classList.add('bg-indigo-100', 'border-indigo-400', 'text-indigo-700');
        } else {
          this.classList.remove('bg-indigo-100', 'border-indigo-400', 'text-indigo-700');
        }
      });
    });
  }

  initializeHomeToneChips();
  initializeHomeToneChipToggles();
});

document.addEventListener('turbo:render', function() {
  // Handle tone chip selection on home page - Turbo compatible
  function initializeHomeToneChips() {
    const toneChips = document.querySelectorAll('input[name="tone_chips[]"]');
    
    toneChips.forEach(chip => {
      // Remove existing event listeners to prevent duplicates
      const newChip = chip.cloneNode(true);
      chip.parentNode.replaceChild(newChip, chip);
      
      newChip.addEventListener('change', function() {
        const span = this.nextElementSibling;
        if (this.checked) {
          span.classList.remove('bg-gray-50', 'border-gray-200', 'text-gray-600');
          span.classList.add('bg-indigo-100', 'border-indigo-300', 'text-indigo-700');
        } else {
          span.classList.remove('bg-indigo-100', 'border-indigo-300', 'text-indigo-700');
          span.classList.add('bg-gray-50', 'border-gray-200', 'text-gray-600');
        }
      });
    });
  }

  // Simple tone chip toggle functionality - Turbo compatible
  function initializeHomeToneChipToggles() {
    const toneChips = document.querySelectorAll('input[name="tone_chips[]"]');
    
    toneChips.forEach(checkbox => {
      // Remove existing event listeners to prevent duplicates
      const newCheckbox = checkbox.cloneNode(true);
      const newSpan = checkbox.nextElementSibling.cloneNode(true);
      checkbox.parentNode.replaceChild(newCheckbox, checkbox);
      newCheckbox.parentNode.replaceChild(newSpan, newCheckbox.nextElementSibling);
      
      const chipSpan = newCheckbox.nextElementSibling;
      
      chipSpan.addEventListener('click', function() {
        newCheckbox.checked = !newCheckbox.checked;
        
        // Visual feedback
        if (newCheckbox.checked) {
          this.classList.add('bg-indigo-100', 'border-indigo-400', 'text-indigo-700');
        } else {
          this.classList.remove('bg-indigo-100', 'border-indigo-400', 'text-indigo-700');
        }
      });
    });
  }

  initializeHomeToneChips();
  initializeHomeToneChipToggles();
});
</script>

<style>
/* Enhanced form display - Main form visible on all devices */
.main-form {
  display: block !important; /* Always visible now */
}

.mobile-cta {
  display: none !important; /* Hidden temporarily */
}

/* Enhanced mobile improvements */
@media (max-width: 640px) {
  .main-form {
    padding: 0 0.5rem;
  }
  
  /* Improved button sizing for mobile */
  button[type="submit"] {
    min-height: 52px;
    font-size: 1.1rem;
  }
  
  /* Better textarea sizing for mobile */
  textarea {
    min-height: 120px;
    font-size: 1rem;
  }
}

/* Smooth transitions for interactive elements */
button, textarea, input[type="checkbox"] {
  transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
}

/* TODO: Réactiver la logique responsive après refactor du chat UX */
/* 
@media (min-width: 768px) {
  .desktop-form {
    display: block !important;
  }
  
  .mobile-cta {
    display: none !important;
  }
}

@media (max-width: 767px) {
  .desktop-form {
    display: none !important;
  }
  
  .mobile-cta {
    display: block !important;
  }
}
*/
</style>
