<div class="min-h-screen bg-gradient-to-br from-indigo-50 via-white to-purple-50">
  <!-- Hero Section -->
  <div class="relative overflow-hidden">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-24">
      <div class="text-center">
        <!-- Hero Icon -->
        <div class="mx-auto h-20 w-20 mb-8 flex items-center justify-center rounded-full bg-gradient-to-r from-indigo-500 to-purple-600">
          <svg class="h-10 w-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.246 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"></path>
          </svg>
        </div>

        <!-- Hero Headline -->
        <h1 class="text-4xl md:text-5xl font-bold text-gray-900 mb-6">
          What should you read next?
        </h1>
        
        <!-- Hero Subtitle -->
        <p class="text-lg md:text-xl text-gray-600 mb-12 max-w-2xl mx-auto">
          Tell us your mood, we'll find your next book in 30 seconds. 
          <span class="font-semibold text-indigo-600">No account required.</span>
        </p>

        <!-- Mood Input Form - Sleek Design -->
        <div class="max-w-xl mx-auto mb-8 sm:mb-12">
          <!-- Desktop Form -->
          <div id="desktop-form" class="desktop-form">
            <%= form_with url: recommendations_path, method: :post, local: true, class: "space-y-5 sm:space-y-6", data: { turbo: false } do %>
              <!-- Context Textarea -->
              <div>
                <label class="block text-sm font-medium text-gray-700 mb-2 text-left">Describe your reading mood</label>
                <%= text_area_tag :context, nil, 
                    rows: 4, 
                    class: "block w-full border-gray-300 rounded-xl shadow-sm focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 text-base resize-none transition-all duration-200",
                    placeholder: "e.g. 'After Sapiens, I want tech & society, optimistic tone, 250–350 pages'",
                    required: true %>
                <p class="mt-2 text-sm text-gray-500 text-left">Be specific about genre, tone, length, or what you want to explore next</p>
              </div>

              <!-- Tone Chips -->
              <div>
                <label class="block text-sm font-medium text-gray-700 mb-3 text-left">Tone & Style</label>
                <div class="grid grid-cols-2 sm:flex sm:flex-wrap gap-3 justify-center">
                  <% ['Deep dive', 'Fast-paced', 'Optimistic', 'Thoughtful', 'Adventure', 'Cozy', 'Challenging', 'Light'].each do |tone| %>
                    <label class="relative">
                      <%= check_box_tag 'tone_chips[]', tone, false, class: 'sr-only' %>
                      <span class="inline-flex items-center justify-center w-full px-4 py-3 rounded-xl text-sm font-medium bg-gray-50 border-2 border-gray-200 text-gray-600 cursor-pointer hover:bg-indigo-50 hover:border-indigo-300 hover:text-indigo-700 transition-all duration-200 active:scale-95">
                        <%= tone %>
                      </span>
                    </label>
                  <% end %>
                </div>
              </div>

              <!-- Submit Button - Sleek Design -->
              <button type="submit" id="home-submit-button" class="w-full bg-gradient-to-r from-indigo-600 to-purple-600 text-white py-4 px-6 rounded-xl font-semibold text-lg hover:from-indigo-700 hover:to-purple-700 transition-all duration-200 flex items-center justify-center shadow-lg hover:shadow-xl active:scale-95">
                <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
                </svg>
                Get My Next Book
              </button>
            <% end %>
          </div>

          <!-- Mobile CTA -->
          <div id="mobile-cta" class="mobile-cta">
            <div class="text-center p-6 bg-gradient-to-r from-indigo-50 to-purple-50 rounded-2xl border border-indigo-100">
                          <!-- TEMPORAIREMENT DÉSACTIVÉ: Interface chat UX
            <div class="mb-4">
              <div class="mx-auto h-16 w-16 flex items-center justify-center rounded-full bg-gradient-to-r from-indigo-500 to-purple-600 mb-4">
                <svg class="h-8 w-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8s9 3.582 9 8z"/>
                </svg>
              </div>
              <h3 class="text-xl font-semibold text-gray-900 mb-2">Chat with your AI Librarian</h3>
              <p class="text-gray-600 mb-4">Simpler, natural conversation, instant recommendations</p>
            </div>
            
            <a href="/recommendations/chat" 
                data-turbo="false"
                class="inline-flex items-center justify-center w-full bg-gradient-to-r from-indigo-600 to-purple-600 text-white py-3 px-6 rounded-lg font-medium text-base hover:from-indigo-700 hover:to-purple-700 transition-all duration-200 shadow-sm">
              <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8s9 3.582 9 8z"/>
              </svg>
              Start Conversation
            </a>
            -->
            
            <!-- TEMPORAIRE: Section remplacée par un message informatif -->
            <div class="mb-4">
              <div class="mx-auto h-16 w-16 flex items-center justify-center rounded-full bg-gradient-to-r from-blue-500 to-green-600 mb-4">
                <svg class="h-8 w-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
                </svg>
              </div>
              <h3 class="text-xl font-semibold text-gray-900 mb-2">Enhanced Form Experience</h3>
              <p class="text-gray-600 mb-4">We're improving our recommendation form for all devices. Mobile chat coming soon!</p>
            </div>
            </div>
          </div>
        </div>

        <!-- Quick Tips - Sleek Design -->
        <div class="max-w-xl mx-auto p-4 sm:p-6 bg-blue-50 rounded-xl border border-blue-100">
          <h4 class="text-sm font-medium text-blue-800 mb-3 text-center sm:text-left">💡 Quick Tips</h4>
          <ul class="text-sm text-blue-700 space-y-2 text-center sm:text-left">
            <li class="flex items-center justify-center sm:justify-start">
              <span class="w-2 h-2 bg-blue-400 rounded-full mr-2 flex-shrink-0"></span>
              Mention specific authors or books you love
            </li>
            <li class="flex items-center justify-center sm:justify-start">
              <span class="w-2 h-2 bg-blue-400 rounded-full mr-2 flex-shrink-0"></span>
              Include your preferred reading time (quick reads vs. deep dives)
            </li>
            <li class="flex items-center justify-center sm:justify-start">
              <span class="w-2 h-2 bg-blue-400 rounded-full mr-2 flex-shrink-0"></span>
              Specify if you want to explore new genres or stick to favorites
            </li>
          </ul>
        </div>

        <!-- Trust Indicators -->
        <div class="mt-8 flex flex-col sm:flex-row flex-wrap justify-center items-center gap-4 sm:gap-6 text-sm text-gray-500">
          <div class="flex items-center">
            <svg class="w-4 h-4 mr-2 text-green-500 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
            </svg>
            <span class="whitespace-nowrap">AI-powered recommendations</span>
          </div>
          <div class="flex items-center">
            <svg class="w-4 h-4 mr-2 text-green-500 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
            </svg>
            <span class="whitespace-nowrap">Basic recommendations free</span>
          </div>
          <div class="flex items-center">
            <svg class="w-4 h-4 mr-2 text-green-500 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
            </svg>
            <span class="whitespace-nowrap">Sign up for personalization</span>
          </div>
        </div>
        
        <!-- TEMPORAIREMENT DÉSACTIVÉ: Chat Interface Link -->
        <!-- <div class="mt-8 text-center">
          <p class="text-sm text-gray-500 mb-3">Want to try our mobile chat interface?</p>
          <a href="/recommendations/chat" 
              data-turbo="false"
              class="inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-lg text-gray-700 bg-white hover:bg-gray-50 transition-colors duration-200">
            <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8s9 3.582 9 8z"/>
            </svg>
            Try Chat Interface
          </a>
        </div> -->
        
        <!-- TEMPORAIRE: Message informatif -->
        <div class="mt-8 text-center">
          <p class="text-sm text-gray-500 mb-3">Mobile chat interface temporarily disabled</p>
          <div class="inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-lg text-gray-400 bg-gray-50 cursor-not-allowed">
            <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
            </svg>
            Coming Soon
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- How It Works Section -->
  <div class="py-24 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-16">
        <h2 class="text-3xl font-bold text-gray-900 mb-4">How it works</h2>
        <p class="text-xl text-gray-600">Get personalized book recommendations in 3 simple steps</p>
      </div>

      <div class="grid md:grid-cols-3 gap-8">
        <!-- Step 1 -->
        <div class="text-center">
          <div class="mx-auto h-16 w-16 mb-6 flex items-center justify-center rounded-full bg-indigo-100 text-indigo-600 font-bold text-2xl">1</div>
          <h3 class="text-xl font-semibold text-gray-900 mb-3">Describe your mood</h3>
          <p class="text-gray-600">Tell us what you're in the mood for, your preferred tone, or what you want to explore next.</p>
        </div>

        <!-- Step 2 -->
        <div class="text-center">
          <div class="mx-auto h-16 w-16 mb-6 flex items-center justify-center rounded-full bg-indigo-100 text-indigo-600 font-bold text-2xl">2</div>
          <h3 class="text-xl font-semibold text-gray-900 mb-3">Get instant results</h3>
          <p class="text-gray-600">Our AI analyzes your preferences and gives you a personalized reading brief with 3 perfect book picks.</p>
        </div>

        <!-- Step 3 -->
        <div class="text-center">
          <div class="mx-auto h-16 w-16 mb-6 flex items-center justify-center rounded-full bg-indigo-100 text-indigo-600 font-bold text-2xl">3</div>
          <h3 class="text-xl font-semibold text-gray-900 mb-3">Enhance & personalize</h3>
          <p class="text-gray-600">Sign up to import your reading history, save books, and get even better recommendations.</p>
        </div>
      </div>
    </div>
  </div>

  <!-- Enhanced Recommendations Section -->
  <div class="py-24 bg-gradient-to-r from-indigo-50 to-purple-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-16">
        <h2 class="text-3xl font-bold text-gray-900 mb-4">Enhanced recommendations</h2>
        <p class="text-xl text-gray-600">Share your reading data for personalized suggestions</p>
      </div>

      <div class="grid md:grid-cols-2 gap-8">
        <!-- Import Goodreads -->
        <div class="bg-white rounded-2xl p-8 shadow-lg">
          <div class="flex items-center mb-4">
            <div class="h-12 w-12 bg-green-100 rounded-lg flex items-center justify-center mr-4">
              <svg class="h-6 w-6 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M9 19l3 3m0 0l3-3m-3 3V10"></path>
              </svg>
            </div>
            <div>
              <h3 class="text-xl font-semibold text-gray-900">Import Goodreads</h3>
              <p class="text-gray-600">Upload your reading history for personalized recommendations</p>
            </div>
          </div>
          
          <% if user_signed_in? %>
            <%= link_to "Import Library", new_import_path, 
                class: "inline-flex items-center px-4 py-2 text-sm font-medium text-green-600 bg-green-50 border border-green-200 rounded-lg hover:bg-green-100 transition-colors duration-200" %>
          <% else %>
            <button onclick="showSignupPopup('Sign up to import your Goodreads library and get personalized recommendations!')" 
                    class="inline-flex items-center px-4 py-2 text-sm font-medium text-green-600 bg-green-50 border border-green-200 rounded-lg hover:bg-green-100 transition-colors duration-200">
              Import Library
            </button>
          <% end %>
        </div>

        <!-- Manual Entry -->
        <div class="bg-white rounded-2xl p-8 shadow-lg">
          <div class="flex items-center mb-4">
            <div class="h-12 w-12 bg-blue-100 rounded-lg flex items-center justify-center mr-4">
              <svg class="h-6 w-6 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
              </svg>
            </div>
            <div>
              <h3 class="text-xl font-semibold text-gray-900">Add Books Manually</h3>
              <p class="text-gray-600">Build your library by adding books you've read or want to read</p>
            </div>
          </div>
          
          <% if user_signed_in? %>
            <%= link_to "Add Book", new_book_path, 
                class: "inline-flex items-center px-4 py-2 text-sm font-medium text-blue-600 bg-blue-50 border border-blue-200 rounded-lg hover:bg-blue-100 transition-colors duration-200" %>
          <% else %>
            <button onclick="showSignupPopup('Sign up to build your personal library and get better recommendations!')" 
                    class="inline-flex items-center px-4 py-2 text-sm font-medium text-blue-600 bg-blue-50 border border-blue-200 rounded-lg hover:bg-blue-100 transition-colors duration-200">
              Add Book
            </button>
          <% end %>
        </div>
      </div>

      <!-- Subtle Sign-Up CTA - Only show for users who haven't shared data yet -->
      <% if user_signed_in? && current_user.books.empty? %>
        <div class="mt-12 text-center">
          <div class="bg-white rounded-2xl p-8 shadow-lg max-w-2xl mx-auto">
            <h3 class="text-xl font-semibold text-gray-900 mb-3">Ready to Personalize?</h3>
            <p class="text-gray-600 mb-6">Share your reading data to get AI recommendations tailored specifically to your taste.</p>
            <div class="flex flex-col sm:flex-row gap-4 justify-center">
              <%= link_to "Import Goodreads", new_import_path, 
                  class: "inline-flex items-center px-6 py-3 bg-gradient-to-r from-indigo-600 to-purple-600 text-white font-medium rounded-lg hover:from-indigo-700 hover:to-purple-700 transition-all duration-200" %>
              <%= link_to "Add Books Manually", new_book_path, 
                  class: "inline-flex items-center px-6 py-3 border border-gray-300 text-gray-700 font-medium rounded-lg hover:bg-gray-50 transition-colors duration-200" %>
            </div>
          </div>
        </div>
      <% end %>

      <!-- Future Features -->
      <div class="mt-12 text-center">
        <p class="text-gray-600 mb-4">Coming soon: Photo uploads of your reading list for instant library building</p>
        <div class="inline-flex items-center px-4 py-2 bg-gray-100 rounded-full text-sm text-gray-600">
          <svg class="w-4 h-4 mr-2" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm1-12a1 1 0 10-2 0v4a1 1 0 00.293.707l2.828 2.829a1 1 0 101.415-1.415L11 9.586V6z" clip-rule="evenodd"></path>
          </svg>
          Photo uploads coming soon
        </div>
      </div>
    </div>
  </div>

  <!-- Quick Actions for Logged Users -->
  <% if user_signed_in? %>
    <div class="py-16 bg-white">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <h2 class="text-2xl font-bold text-gray-900 mb-8 text-center">Quick Actions</h2>
        <div class="flex flex-wrap justify-center gap-4">
          <%= link_to "View My Library", books_path, 
              class: "inline-flex items-center px-6 py-3 text-base font-medium text-gray-700 bg-gray-100 rounded-lg hover:bg-gray-200 transition-colors duration-200" %>
          <%= link_to "Account Settings", edit_user_registration_path, 
              class: "inline-flex items-center px-6 py-3 text-base font-medium text-gray-700 bg-gray-100 rounded-lg hover:bg-gray-200 transition-colors duration-200" %>
        </div>
      </div>
    </div>
  <% else %>
    <!-- Refine & Discover More Section - For First-Time Users -->
    <div class="py-16 bg-gradient-to-r from-indigo-50 to-purple-50">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-12">
          <h2 class="text-3xl font-bold text-gray-900 mb-4">Refine & Discover More</h2>
          <p class="text-xl text-gray-600">Take your reading journey to the next level</p>
        </div>
        
        <div class="grid md:grid-cols-3 gap-8">
          <!-- Refine Recommendations -->
          <div class="text-center">
            <div class="mx-auto h-16 w-16 mb-4 flex items-center justify-center rounded-full bg-indigo-100">
              <svg class="h-8 w-8 text-indigo-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"></path>
              </svg>
            </div>
            <h3 class="text-lg font-semibold text-gray-900 mb-3">Refine Your Results</h3>
            <p class="text-gray-600 mb-4">Not quite what you're looking for? Adjust your preferences and get new recommendations.</p>
            <button onclick="scrollToTop()" class="inline-flex items-center px-4 py-2 bg-indigo-600 text-white text-sm font-medium rounded-lg hover:bg-indigo-700 transition-colors duration-200">
              Try Different Mood
            </button>
          </div>

          <!-- Explore New Genres -->
          <div class="text-center">
            <div class="mx-auto h-16 w-16 mb-4 flex items-center justify-center rounded-full bg-purple-100">
              <svg class="h-8 w-8 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"></path>
              </svg>
            </div>
            <h3 class="text-lg font-semibold text-gray-900 mb-3">Explore New Genres</h3>
            <p class="text-gray-600 mb-4">Step outside your comfort zone and discover books from genres you haven't tried before.</p>
            <button onclick="suggestNewGenre()" class="inline-flex items-center px-4 py-2 bg-purple-600 text-white text-sm font-medium rounded-lg hover:bg-purple-700 transition-colors duration-200">
              Suggest Genre
            </button>
          </div>

          <!-- Build Your Library -->
          <div class="text-center">
            <div class="mx-auto h-16 w-16 mb-4 flex items-center justify-center rounded-full bg-green-100">
              <svg class="h-8 w-8 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
              </svg>
            </div>
            <h3 class="text-lg font-semibold text-gray-900 mb-3">Build Your Library</h3>
            <p class="text-gray-600 mb-4">Import your Goodreads library or add books manually for even better personalized recommendations.</p>
            <button onclick="showSignupPopup('Sign up to import your Goodreads library and get personalized recommendations!')" 
                    class="inline-flex items-center px-4 py-2 bg-green-600 text-white text-sm font-medium rounded-lg hover:bg-green-700 transition-colors duration-200">
              Import Goodreads
            </button>
          </div>
        </div>
      </div>
    </div>
  <% end %>
</div>

<script>
// TEMPORAIREMENT DÉSACTIVÉ: Logique de basculement entre formulaires
// TODO: Réactiver après refactor du chat UX
document.addEventListener('DOMContentLoaded', function() {
  // Function to show correct form based on screen size
  function showCorrectForm() {
    const desktopForm = document.getElementById('desktop-form');
    const mobileCTA = document.getElementById('mobile-cta');
    
    // TEMPORAIRE: Toujours montrer le formulaire principal
    if (desktopForm) desktopForm.style.display = 'block';
    if (mobileCTA) mobileCTA.style.display = 'none';
    
    /* TODO: Réactiver cette logique après refactor
    if (window.innerWidth >= 768) { // md breakpoint
      // Desktop view
      if (desktopForm) desktopForm.style.display = 'block';
      if (mobileCTA) mobileCTA.style.display = 'none';
    } else {
      // Mobile view
      if (desktopForm) desktopForm.style.display = 'none';
      if (mobileCTA) mobileCTA.style.display = 'block';
    }
    */
  }
  
  // Show correct form on load
  showCorrectForm();
  
  // Update on resize (temporairement désactivé)
  // window.addEventListener('resize', showCorrectForm);
  
  // Add form validation and loading states
  const form = document.querySelector('form');
  if (form) {
    form.addEventListener('submit', function(e) {
      // Validate context is not empty
      const context = document.querySelector('textarea[name="context"]').value.trim();
      if (!context) {
        e.preventDefault();
        alert('Please describe what you\'re in the mood for reading.');
        return;
      }
      
      // Show loading state
      const submitBtn = form.querySelector('button[type="submit"]');
      const originalText = submitBtn.innerHTML;
      submitBtn.innerHTML = '<svg class="animate-spin -ml-1 mr-3 h-5 w-5 text-white" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24"><circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle><path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path></svg>Getting recommendations...';
      submitBtn.disabled = true;
      
      // Re-enable button after a delay (in case of errors)
      setTimeout(() => {
        submitBtn.innerHTML = originalText;
        submitBtn.disabled = false;
      }, 10000);
    });
  }
});

// Handle tone chip selection on home page
document.addEventListener('DOMContentLoaded', function() {
  const toneChips = document.querySelectorAll('input[name="tone_chips[]"]');
  
  toneChips.forEach(chip => {
    chip.addEventListener('change', function() {
      const span = this.nextElementSibling;
      if (this.checked) {
        span.classList.remove('bg-gray-50', 'border-gray-200', 'text-gray-600');
        span.classList.add('bg-indigo-100', 'border-indigo-300', 'text-indigo-700');
      } else {
        span.classList.remove('bg-indigo-100', 'border-indigo-300', 'text-indigo-700');
        span.classList.add('bg-gray-50', 'border-gray-200', 'text-gray-600');
      }
    });
  });

  // Function to show a signup popup
  function showSignupPopup(message) {
    const popup = document.createElement('div');
    popup.className = 'fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50';
    
    const popupContent = document.createElement('div');
    popupContent.className = 'bg-white p-6 rounded-lg shadow-xl max-w-md w-full mx-4';
    
    const title = document.createElement('h3');
    title.className = 'text-lg font-medium text-gray-900 mb-2';
    title.textContent = 'Sign Up Required';
    
    const content = document.createElement('p');
    content.className = 'text-sm text-gray-700 mb-4';
    content.textContent = message;
    
    const signupLink = document.createElement('a');
    signupLink.href = '<%= new_user_registration_path %>';
    signupLink.className = 'inline-flex items-center px-6 py-2 bg-gradient-to-r from-indigo-600 to-purple-600 text-white text-sm font-medium rounded-lg hover:from-indigo-700 hover:to-purple-700 transition-all duration-200';
    signupLink.textContent = 'Sign Up Now';
    
    const closeBtn = document.createElement('button');
    closeBtn.className = 'mt-4 px-4 py-2 bg-gray-200 text-gray-800 rounded-lg font-medium hover:bg-gray-300 transition-colors duration-200';
    closeBtn.textContent = 'Close';
    closeBtn.onclick = () => {
      document.body.removeChild(popup);
    };
    
    popupContent.appendChild(title);
    popupContent.appendChild(content);
    popupContent.appendChild(signupLink);
    popupContent.appendChild(closeBtn);
    popup.appendChild(popupContent);
    
    document.body.appendChild(popup);
  }

  // Function to scroll to top of the page
  function scrollToTop() {
    window.scrollTo({ top: 0, behavior: 'smooth' });
  }

  // Function to suggest a new genre
  function suggestNewGenre() {
    const genres = [
      'Science Fiction', 'Fantasy', 'Mystery', 'Thriller', 'Romance', 
      'Historical Fiction', 'Biography', 'Memoir', 'Self-Help', 'Philosophy',
      'Economics', 'Psychology', 'Travel', 'Cooking', 'Art', 'Music',
      'Poetry', 'Drama', 'Horror', 'Western', 'Young Adult', 'Children'
    ];
    
    const randomGenre = genres[Math.floor(Math.random() * genres.length)];
    const context = document.querySelector('textarea[name="context"]');
    
    if (context) {
      context.value = `I want to explore ${randomGenre} books. Something engaging and well-written that will introduce me to this genre.`;
      context.focus();
      
      // Show toast notification
      showToast(`Try exploring ${randomGenre}! I've updated your request.`, 'success');
    }
  }

  // Toast notification function
  function showToast(message, type = 'info') {
    // Create toast element
    const toast = document.createElement('div');
    toast.className = `fixed top-4 right-4 z-50 px-6 py-3 rounded-lg text-white font-medium transform transition-all duration-300 translate-x-full ${
      type === 'success' ? 'bg-green-500' : 
      type === 'error' ? 'bg-red-500' : 
      'bg-blue-500'
    }`;
    toast.textContent = message;
    
    document.body.appendChild(toast);
    
    // Animate in
    setTimeout(() => {
      toast.classList.remove('translate-x-full');
    }, 100);
    
    // Animate out and remove
    setTimeout(() => {
      toast.classList.add('translate-x-full');
      setTimeout(() => {
        document.body.removeChild(toast);
      }, 300);
    }, 3000);
  }
});
</script>

<style>
/* Responsive form display - TEMPORAIREMENT: Formulaire principal visible sur tous les appareils */
.desktop-form {
  display: block !important; /* Toujours visible maintenant */
}

.mobile-cta {
  display: none !important; /* Masqué temporairement */
}

/* Mobile-specific improvements */
@media (max-width: 640px) {
  .desktop-form {
    padding: 0 0.5rem;
  }
  
  /* Better touch targets for mobile */
  input[type="checkbox"] + span {
    min-height: 44px;
    min-width: 44px;
  }
  
  /* Improved button sizing for mobile */
  button[type="submit"] {
    min-height: 48px;
    font-size: 1.1rem;
  }
  
  /* Better textarea sizing for mobile */
  textarea {
    min-height: 120px;
    font-size: 1rem;
  }
}

/* TODO: Réactiver la logique responsive après refactor du chat UX */
/* 
@media (min-width: 768px) {
  .desktop-form {
    display: block !important;
  }
  
  .mobile-cta {
    display: none !important;
  }
}

@media (max-width: 767px) {
  .desktop-form {
    display: none !important;
  }
  
  .mobile-cta {
    display: block !important;
  }
}
*/
</style>
