# Configuration des bases de données cloud
# Copiez ce fichier vers .env et remplissez vos vraies valeurs

# Development et Test - Neon
DATABASE_URL=postgresql://username:password@neon-host/database_name

# Test - Base séparée (optionnel, peut utiliser la même que dev)
# DATABASE_URL_TEST=postgresql://username:password@neon-host/test_database_name

# Production - Render (configuré via les variables d'environnement de Render)
# DATABASE_URL est automatiquement configuré par Render

# Debug Mode - Affiche les informations de debug dans l'UI (développement uniquement)
# debug_mode=true
